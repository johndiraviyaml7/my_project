package com.edge.connector.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MqttConfig {
    // MQTT client is now managed directly by MqttService
    // This config class is kept for potential future configuration beans
}
