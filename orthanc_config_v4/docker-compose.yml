services:

  # ── Orthanc DICOM Server ──────────────────────────────────────────────────
  orthanc:
    image: orthancteam/orthanc:latest
    container_name: quantixmed_orthanc
    ports:
      - "8042:8042"    # HTTP REST API + DICOMweb
      - "4242:4242"    # DICOM protocol
    volumes:
      - orthanc_db:/var/lib/orthanc/db
    environment:
      ORTHANC__NAME: "QuantixMed-Orthanc"
      ORTHANC__DICOM_AET: "QUANTIXMED"
      ORTHANC__DICOM_PORT: "4242"
      ORTHANC__AUTHENTICATION_ENABLED: "true"
      ORTHANC__REGISTERED_USERS: |
        {"admin": "admin"}
      ORTHANC__REMOTE_ACCESS_ALLOWED: "true"
      ORTHANC__HTTP_DESCRIBE_ERRORS: "true"
      ORTHANC__DICOM_WEB__ENABLE: "true"
      ORTHANC__DICOM_WEB__ROOT: "/dicom-web/"
      ORTHANC__DICOM_WEB__ENABLE_WADO: "true"
      ORTHANC__DICOM_WEB__WADO_ROOT: "/wado"
      ORTHANC__DICOM_WEB__PUBLIC_ROOT: "/dicom-web/"
      ORTHANC__CORS_ENABLED: "true"
      ORTHANC__MAXIMUM_STORAGE_SIZE: "0"
      ORTHANC__MAXIMUM_PATIENT_COUNT: "0"
      ORTHANC__STORAGE_COMPRESSION: "false"
      VERBOSE_ENABLED: "true"
    # No custom healthcheck — the image has its own built-in probe.
    # We remove it here to avoid curl-not-found failures on newer Ubuntu images.
    healthcheck:
      disable: true
    restart: unless-stopped

  # ── OHIF Viewer v3 ─────────────────────────────────────────────────────────
  ohif:
    image: ohif/app:v3.9.0
    container_name: quantixmed_ohif
    ports:
      - "3000:80"
    volumes:
      - ./ohif-app-config.js:/usr/share/nginx/html/app-config.js:ro
    # No dependency on Orthanc health — both start independently.
    # OHIF only needs Orthanc when the browser loads a study, not at startup.
    restart: unless-stopped

volumes:
  orthanc_db:
